<img width="1073" height="587" alt="image" src="https://github.com/user-attachments/assets/72859d9c-af7c-4bd7-9a5c-c879f5810210" />


문제: https://www.acmicpc.net/problem/1325

---

### Hint

![pYiWxiLytssa607dt3Okn7_250817_185413](https://github.com/user-attachments/assets/21c4b98b-21d3-4f19-be0f-69c1908009f9)

---

BFS
```java
import java.util.*;
import java.io.*;

class Main {

    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    static StringTokenizer st;
    static int n, m;
    static ArrayList<Integer>[] graph;
    static boolean[] visited;
    static int[] ans;
    public static void main(String[] args) throws IOException {
        
        st = new StringTokenizer(br.readLine());
        n = Integer.parseInt(st.nextToken());
        m = Integer.parseInt(st.nextToken());

        graph = new ArrayList[n + 1];
        ans = new int[n + 1];

        for(int i=1; i<=n; i++) graph[i] = new ArrayList<>();

        for(int i=0; i<m; i++) {
            st = new StringTokenizer(br.readLine());
            int s = Integer.parseInt(st.nextToken());
            int e = Integer.parseInt(st.nextToken());

            graph[e].add(s); // 신뢰 관계를 따져서 역방향으로 재조작
        }

        int max = -1; // 하나의 정점이 해킹시킨 최대 개수
        for(int i=1; i<=n; i++) {
            visited = new boolean[n + 1];
            ans[i] = BFS(i);
            max = Math.max(max, ans[i]);
        }


        for(int i=1; i<=n; i++) {
            if(max == ans[i]) System.out.print(i + " ");
        }

    }    

    // 하나의 정점이 몇개의 정점을 해킹시키는지 세는 BFS
    static int BFS(int node) {
        Queue<Integer> Q = new LinkedList<>();
        Q.offer(node);
        visited[node] = true;

        int cnt = 0; // 해킹 시킨 개수
        while(!Q.isEmpty()) {
            int cur = Q.poll();

            for(int x : graph[cur]) {
                if(!visited[x]) {
                    Q.offer(x);
                    visited[x] = true;
                    cnt++;
                }
            }
        }
        return cnt;
    }
}


```

---

순방향 , 시간 초과 코드 (8%)

```java
import java.util.*;
import java.io.*;

class Main {

    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    static StringTokenizer st;
    static int n, m;
    static ArrayList<Integer>[] graph;
    static boolean[] visited;
    static int[] ans;
    public static void main(String[] args) throws IOException {
        
        st = new StringTokenizer(br.readLine());
        n = Integer.parseInt(st.nextToken());
        m = Integer.parseInt(st.nextToken());

        graph = new ArrayList[n + 1];
        ans = new int[n + 1];

        for(int i=1; i<=n; i++) graph[i] = new ArrayList<>();

        for(int i=0; i<m; i++) {
            st = new StringTokenizer(br.readLine());
            int s = Integer.parseInt(st.nextToken());
            int e = Integer.parseInt(st.nextToken());

            graph[s].add(e); // 순방향
        }

        for(int i=1; i<=n; i++) {
            visited = new boolean[n + 1];
            BFS(i);
        }

        int max = -1;

        for(int i=1; i<=n; i++) max = Math.max(max, ans[i]);

        for(int i=1; i<=n; i++) {
            if(max == ans[i]) System.out.print(i + " ");
        }

    }    

    static void BFS(int node) {
        Queue<Integer> Q = new LinkedList<>();
        Q.offer(node);
        visited[node] = true;

        while(!Q.isEmpty()) {
            int cur = Q.poll();

            for(int x : graph[cur]) {
                if(!visited[x]) {
                    Q.offer(x);
                    visited[x] = true;
                    ans[x]++; 
                }
            }
        }
    }
}


```

---

### 테스트 케이스

1
```
5 4
3 1
3 2
4 3
5 3
```

```
1 2
```
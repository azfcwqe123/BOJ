<img width="1094" height="583" alt="image" src="https://github.com/user-attachments/assets/50495446-4ed9-4787-86b0-22be91ff35c8" />


문제: https://www.acmicpc.net/problem/1707

참고: https://jsw5913.tistory.com/170

---

### Hint

![65Z2XtbfMXs7AMqmyDiDc5_250817_203308](https://github.com/user-attachments/assets/ea3cb7a8-35d7-4547-86c8-d3abe7af0556)

---

DFS

```java
import java.util.*;
import java.io.*;

class Main {

    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    static StringTokenizer st;
    static StringBuilder sb = new StringBuilder();
    static int T, V, E, s, e;
    static ArrayList<Integer>[] graph;
    static boolean[] visisted;
    static int[] color;
    static boolean flag;
    public static void main(String[] args) throws IOException {
        
        T = Integer.parseInt(br.readLine());

        while(T-- > 0) {
            st = new StringTokenizer(br.readLine());
            V = Integer.parseInt(st.nextToken());
            E = Integer.parseInt(st.nextToken());

            graph = new ArrayList[V + 1];
            for(int i=1; i<=V; i++) graph[i] = new ArrayList<>();

            for(int i=0; i<E; i++) {
                st = new StringTokenizer(br.readLine());
                s = Integer.parseInt(st.nextToken());
                e = Integer.parseInt(st.nextToken());

                graph[s].add(e);
                graph[e].add(s);
            }

            color = new int[V + 1];

            // 이분 그래프 가능한지 여부
            flag = true;

            // 주어진 그래프가 1개로 연결돼있다는 보장X, 모든 정점 탐색
            for(int i=1; i<=V; i++) { 
                if(color[i] == 0) DFS(i, 1);
            }

            sb.append((flag == true) ? "YES" : "NO").append("\n");
        }

        System.out.println(sb);
    }    

    static void DFS(int now, int area) {
        color[now] = area;

        for(int next : graph[now]) { 
            if(color[next] == 0) {
                DFS(next, 3 - area);
            }

            // 인접한 영역의 색깔이 똑같다면 이분그래프 X
            else if(color[now] == color[next]) {
                flag = false;
                return;
            }
        }

    }
}


```

---

BFS

```java
import java.util.*;
import java.io.*;

class Main {

    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    static StringTokenizer st;
    static StringBuilder sb = new StringBuilder();
    static int T, V, E, s, e;
    static ArrayList<Integer>[] graph;
    static boolean[] visisted;
    static int[] color;
    static boolean flag;
    public static void main(String[] args) throws IOException {
        
        T = Integer.parseInt(br.readLine());

        while(T-- > 0) {
            st = new StringTokenizer(br.readLine());
            V = Integer.parseInt(st.nextToken());
            E = Integer.parseInt(st.nextToken());

            graph = new ArrayList[V + 1];
            for(int i=1; i<=V; i++) graph[i] = new ArrayList<>();

            for(int i=0; i<E; i++) {
                st = new StringTokenizer(br.readLine());
                s = Integer.parseInt(st.nextToken());
                e = Integer.parseInt(st.nextToken());

                graph[s].add(e);
                graph[e].add(s);
            }

            color = new int[V + 1];

            flag = true;
            
            // 주어진 그래프가 1개로 연결돼있다는 보장X, 모든 정점 탐색
            for(int i=1; i<=V; i++) {
                if(color[i] == 0) BFS(i, 1);
            }

            sb.append((flag == true) ? "YES" : "NO").append("\n");
        }

        System.out.println(sb);
    }    

    static void BFS(int now, int area) {
        Queue<Integer> Q = new LinkedList<>();
        Q.offer(now);
        color[now] = area;

        while(!Q.isEmpty()) {
            int cur = Q.poll();

            for(int next : graph[cur]) {
                if(color[next] == 0) {
                    color[next] = 3 - color[cur];
                    Q.offer(next);
                }
                
                // 인접한 영역의 색깔이 똑같다면 이분그래프 X
                else if(color[next] == color[cur]) {
                    flag = false;
                    return;
                }
            }
        }

    }
}


```

---

### 테스트 케이스

1
```
2
3 2
1 3
2 3
4 4
1 2
2 3
3 4
4 2
```

```
YES
NO
```

